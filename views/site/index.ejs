<!-- <html>
<head>
  <title>
  </title>

</head>
<body> -->
  <!-- <h1> homepage</h1>

  <form method="post" action="/zip"> -->
                              <!-- req.body is coming from author -->
      <!-- <input type="text" name="author[firstName]" placeholder="zipcode"<
      
      <button> Search </button>
    </form> -->

<!-- </body>
</html> -->

<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title>Geocoding service</title>
     <!--<style>
      html, body, #map-canvas {
        height: 100%;
        margin: 0px;
        padding: 0px
      }
      #panel {
        position: absolute;
        top: 5px;
        left: 50%;
        margin-left: -180px;
        z-index: 5;
        background-color: #fff;
        padding: 5px;
        border: 1px solid #999;
      }
    </style> -->
    
    <script src="https://maps.googleapis.com/maps/api/js?v=3.exp"></script>
    <script>
    window.onload = function (){
var geocoder;
var map;
var initialize = function () {
  geocoder = new google.maps.Geocoder();
  var latlng = new google.maps.LatLng(-34.397, 150.644);
  var mapOptions = {
    zoom: 8,
    center: latlng
  }
  map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);
}

var codeAddress = function () {
  var address = document.getElementById('address').value;
  geocoder.geocode( { 'address': address}, function(results, status) {
    if (status == google.maps.GeocoderStatus.OK) {
      map.setCenter(results[0].geometry.location);
      var marker = new google.maps.Marker({
          map: map,
          position: results[0].geometry.location
      });
      alert('this is the location');
    } else {
      alert('Geocode was not successful for the following reason: ' + status);
    }
  });
}
google.maps.event.addDomListener(window, 'load', initialize);

document.getElementById('mapButton').onclick = function() { initialize(); console.log(geocoder); return codeAddress; };
}
    </script>
  </head>
  <body>
    <div id="panel">
      <input id="address" type="textbox" placeholder="city">
      <input id="mapButton" type="button" value="search area for gardens" onclick="codeAddress()">
    </div>
    <div id="map-canvas"></div>
  </body>
</html>
